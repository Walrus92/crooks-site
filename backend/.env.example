# Para docker compose local (servicio "db")
# Ajusta el puerto a lo que pongas en .env.db (por defecto 5433)
DATABASE_URL="postgresql://crooks:change-me@localhost:5433/crooks?schema=public"

# JWT, mail, etc. (si los tienes)
JWT_SECRET="change-me"
SMTP_URL=""
